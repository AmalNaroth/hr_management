import 'package:flutter/material.dart';

class EmployeesLeaveScreen extends StatelessWidget {
  const EmployeesLeaveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: ListView(
        children: const [
          
        ],
      ),
      //drawer: DrawerCardWidget(),
    );
  }
}